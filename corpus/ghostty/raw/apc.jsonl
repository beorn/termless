{"suite":"ghostty","file":"apc.zig","testName":"unknown APC command","sourceLine":233,"zigBody":"test \"unknown APC command\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"Xabcdef1234\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"garbage Kitty command","sourceLine":243,"zigBody":"test \"garbage Kitty command\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"Gabcdef1234\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"Kitty command with overflow u32","sourceLine":255,"zigBody":"test \"Kitty command with overflow u32\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"Ga=p,i=10000000000\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"Kitty command with overflow i32","sourceLine":267,"zigBody":"test \"Kitty command with overflow i32\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"Ga=p,i=1,z=-9999999999\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"kitty feed error deinits parser","sourceLine":279,"zigBody":"test \"kitty feed error deinits parser\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    // Feed a valid kitty command start to allocate parser state, then\n    // trigger an error during feed via an integer overflow. The testing\n    // allocator will detect leaks if deinit is not called.\n    var h: Handler = .{};\n    defer h.deinit();\n    h.start();\n    for (\"Ga=p,i=10000000000;\") |c| h.feed(alloc, c);\n    try testing.expect(h.state == .ignore);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"kitty max bytes exceeded","sourceLine":295,"zigBody":"test \"kitty max bytes exceeded\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{ .max_bytes = .init(.{ .kitty = 4 }) };\n    defer h.deinit();\n    h.start();\n    // 'G' identifies kitty, 'a=t;' moves to data state, then feed exceeds max_bytes.\n    for (\"Ga=t;\") |c| h.feed(alloc, c);\n    try testing.expect(h.state != .ignore);\n    for (\"abcd\") |c| h.feed(alloc, c);\n    try testing.expect(h.state != .ignore);\n    // The 5th data byte exceeds the 4-byte limit.\n    h.feed(alloc, 'e');\n    try testing.expect(h.state == .ignore);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"valid Kitty command","sourceLine":314,"zigBody":"test \"valid Kitty command\" {\n    if (comptime !build_options.kitty_graphics) return error.SkipZigTest;\n\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    const input = \"Gf=24,s=10,v=20,hello=world\";\n    for (input) |c| h.feed(alloc, c);\n\n    var cmd = h.end().?;\n    defer cmd.deinit(alloc);\n    try testing.expect(cmd == .kitty);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"identify with unrecognized command","sourceLine":330,"zigBody":"test \"identify with unrecognized command\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"abcd;payload\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"identify buffer overflow","sourceLine":340,"zigBody":"test \"identify buffer overflow\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"abcde;payload\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"identify with no input","sourceLine":350,"zigBody":"test \"identify with no input\" {\n    const testing = std.testing;\n\n    var h: Handler = .{};\n    h.start();\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"identify with unknown partial input","sourceLine":358,"zigBody":"test \"identify with unknown partial input\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"25a\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"garbage glyph command","sourceLine":368,"zigBody":"test \"garbage glyph command\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"25a1;X\") |c| h.feed(alloc, c);\n\n    try testing.expect(h.end() == null);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"valid glyph command","sourceLine":379,"zigBody":"test \"valid glyph command\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"25a1;q;cp=E0A0\") |c| h.feed(alloc, c);\n\n    var cmd = h.end().?;\n    defer cmd.deinit(alloc);\n    try testing.expect(cmd == .glyph);\n    try testing.expect(cmd.glyph == .query);\n}"}
{"suite":"ghostty","file":"apc.zig","testName":"disabled glyph command is ignored","sourceLine":393,"zigBody":"test \"disabled glyph command is ignored\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.enable(.glyph, false);\n    h.start();\n    for (\"25a1;q;cp=e0a0\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"}
