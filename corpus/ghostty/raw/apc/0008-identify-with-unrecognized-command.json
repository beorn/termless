{
  "suite": "ghostty",
  "file": "apc.zig",
  "testName": "identify with unrecognized command",
  "sourceLine": 330,
  "zigBody": "test \"identify with unrecognized command\" {\n    const testing = std.testing;\n    const alloc = testing.allocator;\n\n    var h: Handler = .{};\n    h.start();\n    for (\"abcd;payload\") |c| h.feed(alloc, c);\n    try testing.expect(h.end() == null);\n}"
}
