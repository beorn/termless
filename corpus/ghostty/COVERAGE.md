# ghostty corpus extraction coverage

Generated by `extract.ts` from https://github.com/ghostty-org/ghostty. Regenerate with `bun extract.ts <path-to-zig-files>`.

## Totals

| File                  | Raw test blocks | Auto-converted cases |
| --------------------- | --------------: | -------------------: |
| Terminal.zig          |             385 |                    4 |
| Screen.zig            |             194 |                    0 |
| PageList.zig          |             211 |                    0 |
| page.zig              |              47 |                    0 |
| stream.zig            |              37 |                    0 |
| stream_terminal.zig   |              67 |                   12 |
| Parser.zig            |              24 |                    0 |
| Selection.zig         |              18 |                    0 |
| SelectionGesture.zig  |              31 |                    0 |
| ScreenSet.zig         |               1 |                    0 |
| formatter.zig         |             100 |                    8 |
| style.zig             |              38 |                    0 |
| sgr.zig               |              31 |                    0 |
| modes.zig             |               7 |                    0 |
| color.zig             |              22 |                    0 |
| apc.zig               |              14 |                    0 |
| dcs.zig               |               8 |                    0 |
| device_attributes.zig |               7 |                    0 |
| device_status.zig     |               2 |                    0 |
| size.zig              |               5 |                    0 |
| size_report.zig       |               5 |                    0 |
| focus.zig             |               2 |                    0 |
| mouse.zig             |               1 |                    0 |
| render.zig            |              18 |                    0 |
| Tabstops.zig          |               5 |                    0 |
| UTF8Decoder.zig       |               3 |                    0 |
| StringMap.zig         |               3 |                    0 |
| x11_color.zig         |               1 |                    0 |
| **Total**             |        **1287** |               **24** |

Conversion is scoped to three files whose tests consistently follow a fully mechanical shape - init with literal `cols`/`rows`, feed input through a narrow whitelisted call, assert once via a plain-text dump:

- **`Terminal.zig`** - `Terminal.init(alloc, .{ .cols = N, .rows = M })` + print/printString-only input + `t.plainString()` dump assertion.
- **`stream_terminal.zig`** - the same `Terminal.init` shape, wrapped in a `Stream` (`.initAlloc(alloc, .init(&t))`) fed via `s.nextSlice("literal bytes")` - including real escape sequences like `\x1B[1;1H`. This is the closer analog to genuine emulator conformance input: no Zig-method-to-VT-sequence translation is needed since the literal _is_ the wire format. Two narrow accessor-read-then-compare extensions also convert here: `t.getTitle()` -> `expectedTitle`, and `t.modes.get(.NAME)` -> a multi-phase `steps` case of `expectedModes` (mode state has no single dump to assert, so it converts to feed/assert/feed/assert phases instead of a flat case).
- **`formatter.zig`** - the same `Terminal.init` + `t.vtStream()` + `s.nextSlice("literal bytes")` input shape, but asserted via `PageFormatter`'s full-viewport `.plain` emit (bare `.init(page, .plain)`, no rectangle/unwrap/trim/html/vt config) instead of `t.plainString()` - verified byte-for-byte equivalent trim semantics (row join, trailing-whitespace trim, trailing-blank-row drop) before this converter was written. Any other `PageFormatter` config diverges from that semantics (e.g. `.unwrap = true` collapses a soft-wrap row split a real viewport read would show) and is rejected, not converted.

Every other file needs a distinct future converter, not a variant of any shape above: `Screen.zig` tests use a lower-level page/pin API and a different input method (`s.testWriteString`); `PageList.zig` and `page.zig` operate on pages/pins directly with no VT input at all; `Parser.zig` asserts on parser state-machine transitions (no screen); `stream.zig` dispatches into a custom capture-handler struct rather than a real `Terminal`; the remaining files (`Selection*.zig`, `style.zig`, `sgr.zig`, `modes.zig`, `color.zig`, `apc.zig`, `dcs.zig`, `device_*.zig`, `size*.zig`, `focus.zig`, `mouse.zig`, `render.zig`, `Tabstops.zig`, `UTF8Decoder.zig`, `StringMap.zig`, `x11_color.zig`, `ScreenSet.zig`) test protocol encoding or internal state directly rather than input-to-screen-output behavior.

## Terminal.zig: why the remaining 381 weren't converted

| Reason                                                                                                  | Count |
| ------------------------------------------------------------------------------------------------------- | ----: |
| uses t.setCursorPos() - outside the print/printString/plainString whitelist                             |    92 |
| uses t.modes.set() - outside the print/printString/plainString whitelist                                |    71 |
| uses t.carriageReturn() - outside the print/printString/plainString whitelist                           |    57 |
| uses t.setProtectedMode() - outside the print/printString/plainString whitelist                         |    23 |
| uses t.setAttribute() - outside the print/printString/plainString whitelist                             |    18 |
| uses t.semanticPrompt() - outside the print/printString/plainString whitelist                           |    17 |
| uses t.setTopAndBottomMargin() - outside the print/printString/plainString whitelist                    |    14 |
| uses t.screens.active.startHyperlink() - outside the print/printString/plainString whitelist            |    13 |
| uses t.screens.active.pages.getCell() - outside the print/printString/plainString whitelist             |    11 |
| init() has non-cols/rows options: {cols,max_scrollback,rows}                                            |     9 |
| uses t.switchScreenMode() - outside the print/printString/plainString whitelist                         |     6 |
| init() cols/rows are non-literal expressions (variables, not numbers)                                   |     4 |
| uses t.configureCharset() - outside the print/printString/plainString whitelist                         |     4 |
| no expectEqualStrings(...) assertion pairs with a plainString-bound variable                            |     3 |
| uses t.cursorRight() - outside the print/printString/plainString whitelist                              |     3 |
| uses t.printRepeat() - outside the print/printString/plainString whitelist                              |     3 |
| uses t.printSlice() - outside the print/printString/plainString whitelist                               |     3 |
| zero or multiple Terminal instances in one test                                                         |     2 |
| uses t.horizontalTab() - outside the print/printString/plainString whitelist                            |     2 |
| uses t.index() - outside the print/printString/plainString whitelist                                    |     2 |
| uses t.cursorLeft() - outside the print/printString/plainString whitelist                               |     2 |
| uses t.cursorDown() - outside the print/printString/plainString whitelist                               |     2 |
| uses t.deleteChars() - outside the print/printString/plainString whitelist                              |     2 |
| uses t.eraseLine() - outside the print/printString/plainString whitelist                                |     2 |
| uses t.cursorIsAtPrompt() - outside the print/printString/plainString whitelist                         |     2 |
| reads external resource via @embedFile                                                                  |     1 |
| print/printString called with a non-literal argument (variable, numeric codepoint, or other expression) |     1 |
| uses t.linefeed() - outside the print/printString/plainString whitelist                                 |     1 |
| uses t.backspace() - outside the print/printString/plainString whitelist                                |     1 |
| uses t.insertLines() - outside the print/printString/plainString whitelist                              |     1 |
| uses t.eraseChars() - outside the print/printString/plainString whitelist                               |     1 |
| uses t.cursorUp() - outside the print/printString/plainString whitelist                                 |     1 |
| uses t.deleteLines() - outside the print/printString/plainString whitelist                              |     1 |
| uses t.tabClear() - outside the print/printString/plainString whitelist                                 |     1 |
| uses t.fullReset() - outside the print/printString/plainString whitelist                                |     1 |
| terminal variable isn't named `t`, or init() call shape not recognized                                  |     1 |
| uses t.screens.active.pages.trackPin() - outside the print/printString/plainString whitelist            |     1 |
| uses t.resize() - outside the print/printString/plainString whitelist                                   |     1 |
| uses t.glyphProtocol() - outside the print/printString/plainString whitelist                            |     1 |

## stream_terminal.zig: why the remaining 55 weren't converted

| Reason                                                                                                                                 | Count |
| -------------------------------------------------------------------------------------------------------------------------------------- | ----: |
| Stream isn't initialized exactly once as `.initAlloc(alloc, .init(&t))` (custom capture handler, different shape, or multiple streams) |    29 |
| no expectEqualStrings(...) assertion pairs with a plainString-bound variable                                                           |    10 |
| uses t.colors.palette.mask.isSet() - outside the plainString whitelist                                                                 |     4 |
| uses t.colors.foreground.get() - outside the plainString whitelist                                                                     |     3 |
| uses t.colors.background.get() - outside the plainString whitelist                                                                     |     2 |
| uses t.colors.cursor.get() - outside the plainString whitelist                                                                         |     2 |
| uses t.fullReset() - outside the plainString whitelist                                                                                 |     2 |
| uses t.glyph_glossary.contains() - outside the plainString whitelist                                                                   |     1 |
| direct field assignment on the terminal                                                                                                |     1 |
| init() has non-cols/rows options: {cols,max_scrollback,rows}                                                                           |     1 |

## formatter.zig: why the remaining 92 weren't converted

| Reason                                                                                                                                                                       | Count |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----: |
| PageFormatter isn't constructed exactly once as `.init(page, .plain)` (different emit mode - .vt/.html - or a `.{ .emit = .plain, ... }` config, e.g. unwrap/trim/rectangle) |    64 |
| sets formatter.start_x - outside the full-viewport/plain whitelist (point_map is instrumentation-only and allowed)                                                           |     7 |
| sets formatter.start_y - outside the full-viewport/plain whitelist (point_map is instrumentation-only and allowed)                                                           |     6 |
| sets formatter.end_y - outside the full-viewport/plain whitelist (point_map is instrumentation-only and allowed)                                                             |     3 |
| uses t.screens.active.pages.pin() - outside the vtStream/deinit whitelist                                                                                                    |     3 |
| sets formatter.end_x - outside the full-viewport/plain whitelist (point_map is instrumentation-only and allowed)                                                             |     2 |
| sets formatter.trailing_state - outside the full-viewport/plain whitelist (point_map is instrumentation-only and allowed)                                                    |     2 |
| uses t.screens.active.cursor.style.eql() - outside the vtStream/deinit whitelist                                                                                             |     1 |
| uses t.screens.active.kitty_keyboard.current() - outside the vtStream/deinit whitelist                                                                                       |     1 |
| uses t.screens.active.charset.charsets.get() - outside the vtStream/deinit whitelist                                                                                         |     1 |
| uses t.modes.get() - outside the vtStream/deinit whitelist                                                                                                                   |     1 |
| uses t.tabstops.get() - outside the vtStream/deinit whitelist                                                                                                                |     1 |

## Files with no registered converter (raw-only extraction)

Every block from these files is in `raw/` but none were attempted for conversion - see the file-by-file rationale above the totals table. The category breakdown below is the signal for which one to target next.

| File                  | Raw test blocks |
| --------------------- | --------------: |
| Screen.zig            |             194 |
| PageList.zig          |             211 |
| page.zig              |              47 |
| stream.zig            |              37 |
| Parser.zig            |              24 |
| Selection.zig         |              18 |
| SelectionGesture.zig  |              31 |
| ScreenSet.zig         |               1 |
| style.zig             |              38 |
| sgr.zig               |              31 |
| modes.zig             |               7 |
| color.zig             |              22 |
| apc.zig               |              14 |
| dcs.zig               |               8 |
| device_attributes.zig |               7 |
| device_status.zig     |               2 |
| size.zig              |               5 |
| size_report.zig       |               5 |
| focus.zig             |               2 |
| mouse.zig             |               1 |
| render.zig            |              18 |
| Tabstops.zig          |               5 |
| UTF8Decoder.zig       |               3 |
| StringMap.zig         |               3 |
| x11_color.zig         |               1 |

## Non-converted remainder by category (all files, heuristic by test name)

This is the signal for what converter to write next.

| Category                                  | Count |
| ----------------------------------------- | ----: |
| other / uncategorized                     |   325 |
| style / SGR / attributes                  |   190 |
| resize / reflow                           |   132 |
| erase / delete / insert (editing ops)     |   100 |
| selection                                 |    99 |
| scroll / scrollback / scrolling region    |    94 |
| cursor position / movement                |    70 |
| grapheme / unicode / wide chars           |    49 |
| hyperlink                                 |    43 |
| screen read/write/page internals          |    35 |
| mode / DEC private modes                  |    31 |
| kitty graphics                            |    23 |
| stream handler dispatch                   |    22 |
| parser state machine (low-level VT bytes) |    12 |
| tabstops                                  |    11 |
| dirty-tracking / rendering internals      |    10 |
| semantic prompt / shell integration       |    10 |
| charset                                   |     5 |
| alt screen switching                      |     2 |
