{
  "ghostty::ghostty/stream_terminal.zig::window_title effect not called without callback": "ghostty-web's headless GhosttyTerminal exposes no title surface (no getTitle/onTitleChange; only the DOM-oriented Terminal wrapper has onTitleChange), so the backend's title tracking is init/reset-only. Upstream ghostty-web API gap — not fixable in the backend adapter.",
  "ghostty::libvterm/13state_edit.test::IL": "IL (insert line) within the scrolling region.",
  "ghostty::libvterm/16state_resize.test::Resize grow doesn't cancel phantom": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "ghostty::libvterm/20state_wrapping.test::80th column causes linefeed on wraparound": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "ghostty::libvterm/20state_wrapping.test::80th Column Phantom": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "ghostty::libvterm/20state_wrapping.test::80th column phantom linefeed phantom cancelled by explicit cursor move": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "ghostty::libvterm/21state_tabstops.test::TBC 0": "Tab-stop set/clear (HTS/TBC).",
  "ghostty::libvterm/28state_dbl_wh.test::Double Width, Single Height": "DECDWL/DECDHL double-width and double-height line attributes.",
  "ghostty::libvterm/63screen_resize.test::Resize taller attempts to pop scrollback": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "ghostty::libvterm/63screen_resize.test::Resize wider allows print in new area": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "ghostty::libvterm/69screen_reflow.test::Resize narrower can create continuation lines": "Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "ghostty::libvterm/69screen_reflow.test::Shell wrapped prompt behaviour": "Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "ghostty::libvterm/90vttest_01-movement-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_01-movement-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_01-movement-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_01-movement-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_02-screen-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_02-screen-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_02-screen-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "ghostty::libvterm/90vttest_02-screen-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::ghostty/stream_terminal.zig::alt screen": "VT100-era engine: DECSET 1049 alternate screen unsupported. Era gap — not a fix target.",
  "vt100::ghostty/stream_terminal.zig::charsets": "VT100-era engine: ESC ( 0 charset designator unsupported (leaks '0', no special-graphics translation). Era gap — @termless/vt100 is deliberately VT220-era; not a fix target.",
  "vt100::ghostty/stream_terminal.zig::DECALN screen alignment": "VT100-era engine: ESC # 8 DECALN unsupported (renders '8'). Era gap — not a fix target.",
  "vt100::libvterm/11state_movecursor.test::Backspace cancels phantom": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "vt100::libvterm/11state_movecursor.test::Cursor Backward Tab": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Tab movement (CHT/CBT).",
  "vt100::libvterm/11state_movecursor.test::Cursor Horizontal Tab": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Tab movement (CHT/CBT).",
  "vt100::libvterm/11state_movecursor.test::Horizontal Position Absolute": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vt100::libvterm/11state_movecursor.test::Horizontal Position Backward": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vt100::libvterm/11state_movecursor.test::Horizontal Position Relative": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vt100::libvterm/11state_movecursor.test::Vertical Position Relative": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vt100::libvterm/12state_scroll.test::Linefeed outside DECSTBM": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vt100::libvterm/12state_scroll.test::Reverse Index in DECSTBM": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vt100::libvterm/15state_mode.test::DEC origin mode": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "vt100::libvterm/15state_mode.test::Newline/Linefeed mode": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vt100::libvterm/15state_mode.test::Origin mode bounds cursor to scrolling region": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "vt100::libvterm/15state_mode.test::Origin mode with DECSLRM": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "vt100::libvterm/20state_wrapping.test::DEC Auto Wrap Mode": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "vt100::libvterm/21state_tabstops.test::HTS": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Tab-stop set/clear (HTS/TBC).",
  "vt100::libvterm/21state_tabstops.test::TBC 3": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Tab-stop set/clear (HTS/TBC).",
  "vt100::libvterm/22state_save.test::Restore": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSC/DECRC cursor save/restore.",
  "vt100::libvterm/22state_save.test::Save twice, restore twice happens on both edge transitions": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSC/DECRC cursor save/restore.",
  "vt100::libvterm/27state_reset.test::RIS clears tabstops": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). RIS (full reset) does not clear custom tab stops.",
  "vt100::libvterm/28state_dbl_wh.test::Double Height": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/28state_dbl_wh.test::Double Width, Single Height": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/60screen_ascii.test::Altscreen": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSET 1049 alternate screen.",
  "vt100::libvterm/61screen_unicode.test::Outputing CJK doublewidth in 80th column should wraparound to next line and not crash\"": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). CJK double-width wraparound at the last column.",
  "vt100::libvterm/62screen_damage.test::Merge scroll*2 with damage": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Damage-rect merging across scrolls.",
  "vt100::libvterm/63screen_resize.test::Resize can operate on altscreen": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vt100::libvterm/63screen_resize.test::Resize shorter does not lose line with cursor": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vt100::libvterm/63screen_resize.test::Resize shorter with content must scroll": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vt100::libvterm/63screen_resize.test::Resize taller attempts to pop scrollback": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vt100::libvterm/64screen_pen.test::Italic": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SGR pen attribute tracking (italic/underline).",
  "vt100::libvterm/64screen_pen.test::Underline": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SGR pen attribute tracking (italic/underline).",
  "vt100::libvterm/65screen_protect.test::Selective erase": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECSCA/DECSED selective erase of protected cells.",
  "vt100::libvterm/67screen_dbl_wh.test::Double Height": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/67screen_dbl_wh.test::Double Width, Single Height": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/67screen_dbl_wh.test::DWL doesn't spill over on scroll": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/67screen_dbl_wh.test::Single Width, Single Height": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). DECDWL/DECDHL double-width and double-height line attributes.",
  "vt100::libvterm/69screen_reflow.test::Resize narrower can create continuation lines": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "vt100::libvterm/69screen_reflow.test::Resize wider reflows wide lines": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "vt100::libvterm/69screen_reflow.test::Shell wrapped prompt behaviour": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "vt100::libvterm/90vttest_01-movement-1.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_01-movement-2.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_01-movement-3.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_01-movement-4.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_02-screen-1.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_02-screen-2.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_02-screen-3.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vt100::libvterm/90vttest_02-screen-4.test::Output": "VT100-era engine (@termless/vt100 is deliberately VT220-era; era gap, not a fix target). SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::ghostty/stream_terminal.zig::charsets": "DEC Special Graphics (ESC ( 0) not translated — backtick renders raw instead of ◆. Real vterm V1 parity gap; graduate to an implementation bead.",
  "vterm::libvterm/11state_movecursor.test::Backspace cancels phantom": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "vterm::libvterm/11state_movecursor.test::Horizontal Position Relative": "Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vterm::libvterm/11state_movecursor.test::Vertical Position Relative": "Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "vterm::libvterm/12state_scroll.test::Linefeed outside DECSTBM": "DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vterm::libvterm/12state_scroll.test::Reverse Index in DECSTBM": "DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vterm::libvterm/15state_mode.test::Newline/Linefeed mode": "DECSTBM scrolling-region edge behavior for LF/RI, and LNM newline mode.",
  "vterm::libvterm/15state_mode.test::Origin mode bounds cursor to scrolling region": "Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "vterm::libvterm/15state_mode.test::Origin mode with DECSLRM": "Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "vterm::libvterm/16state_resize.test::Resize shrink moves cursor": "THE ONE VTERM-ONLY GAP in this suite. On shrink, vterm REFLOWS and follows the cursor onto the continuation row (got row 1) where libvterm, xterm.js and vt100 clamp it to the last column of row 0. Decide which is intended before changing it — vterm's behavior may be the deliberate one, and this is the same class as the deferred-wrap ruling.",
  "vterm::libvterm/20state_wrapping.test::DEC Auto Wrap Mode": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "vterm::libvterm/28state_dbl_wh.test::Double Width, Single Height": "DECDWL/DECDHL double-width and double-height line attributes.",
  "vterm::libvterm/63screen_resize.test::Resize shorter does not lose line with cursor": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vterm::libvterm/63screen_resize.test::Resize taller attempts to pop scrollback": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "vterm::libvterm/65screen_protect.test::Selective erase": "DECSCA/DECSED selective erase of protected cells.",
  "vterm::libvterm/90vttest_01-movement-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_01-movement-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_01-movement-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_01-movement-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_02-screen-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_02-screen-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_02-screen-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "vterm::libvterm/90vttest_02-screen-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::ghostty/Terminal.zig::Terminal: input that forces scroll": "xterm folds wrap+scroll at exact width differently than ghostty (got 'ab/cd/ef', upstream expects scrolled single column; the ghostty backend PASSES this case). Investigate converter vs engine semantics before blaming either.",
  "xterm::libvterm/11state_movecursor.test::Backspace cancels phantom": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "xterm::libvterm/11state_movecursor.test::Horizontal Position Backward": "Relative/absolute cursor positioning (CSI a / CSI e / CSI G family) not fully implemented.",
  "xterm::libvterm/13state_edit.test::IL": "IL (insert line) within the scrolling region.",
  "xterm::libvterm/15state_mode.test::Origin mode bounds cursor to scrolling region": "Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "xterm::libvterm/15state_mode.test::Origin mode with DECSLRM": "Origin mode (DECOM) cursor clamping to the scrolling region, incl. DECSLRM interaction.",
  "xterm::libvterm/20state_wrapping.test::DEC Auto Wrap Mode": "Deferred-wrap (phantom column) cursor convention: libvterm reports the cursor ON the last column with a separate pending-wrap flag; this engine reports col == cols, one PAST the last column. Not a rendering difference — the glyphs match. Decide the reporting convention before treating it as an engine bug.",
  "xterm::libvterm/28state_dbl_wh.test::Double Width, Single Height": "DECDWL/DECDHL double-width and double-height line attributes.",
  "xterm::libvterm/63screen_resize.test::Resize shorter with content must scroll": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "xterm::libvterm/63screen_resize.test::Resize taller attempts to pop scrollback": "Resize semantics beyond reflow: scrolling content off on shrink, popping scrollback on grow, preserving the cursor's line, and resize while on the alternate screen.",
  "xterm::libvterm/69screen_reflow.test::Resize narrower can create continuation lines": "Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "xterm::libvterm/69screen_reflow.test::Resize wider reflows wide lines": "Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "xterm::libvterm/69screen_reflow.test::Shell wrapped prompt behaviour": "Reflow/rewrap when the width changes: wrapped lines must re-join when widened and re-split when narrowed, carrying the continuation flag and the cursor with them. vterm PASSES all four of these.",
  "xterm::libvterm/90vttest_01-movement-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_01-movement-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_01-movement-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_01-movement-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_02-screen-1.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_02-screen-2.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_02-screen-3.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine.",
  "xterm::libvterm/90vttest_02-screen-4.test::Output": "SUSPECTED CONVERTER ARTIFACT, not an engine gap: every backend fails this, ghostty included (which passes 90% of the suite). These vttest scenarios paint the whole screen through $SEQ loops and file-level setup; the converter most likely under-models their prefix. Investigate the converter before blaming any engine."
}
